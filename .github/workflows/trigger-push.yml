name: Trigger push

on:
  push:
    branches:
      - 'main'
      - 'staging'
    paths:
      - '**.tf'
jobs:
  job-push:
    runs-on: ubuntu-latest
    steps:

      - name: Printa push
        run: echo "job triggado por push na branch main"