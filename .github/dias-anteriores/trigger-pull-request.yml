name: Trigger pull reques

on:
  pull_request:
    types: [opened, edited, closed, reopened]
    branches:
      - 'main'
      - 'main*'
      - 'main/**'
jobs:
  job-pull-request:
    runs-on: ubuntu-latest
    steps:

      - name: Printa pull request
        run: echo "job triggado por pull request na branch main"